{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "passThroughEnv": [
        "NEXT_PUBLIC_*",
        "SUPABASE_*",
        "OPENAI_API_KEY",
        "SLACK_WEBHOOK_URL"
      ]
    }
  }
}
